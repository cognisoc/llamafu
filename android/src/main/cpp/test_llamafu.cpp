#include "llamafu.h"
#include <iostream>
#include <string>

// Simple test to verify the library can be compiled and linked
int main() {
    std::cout << "Llamafu test program" << std::endl;
    
    // This is just a simple test to verify compilation and linking
    // We're not actually calling the functions because we don't have a model file
    // and we're not running on Android
    
    std::cout << "Llamafu library compiled and linked successfully!" << std::endl;
    return 0;
}